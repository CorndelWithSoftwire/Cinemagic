import * as React from 'react';

/**
 * Loading spinner to be used while fetching data
 */
export default function LoadingSpinner() {
    return (
      <div className="spinner-container">
        <div className="spinner" />
      </div>
    );
}
