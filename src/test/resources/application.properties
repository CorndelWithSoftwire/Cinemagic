# Use embedded H2 database
spring.datasource.url = jdbc:h2:mem:test;MODE=MYSQL;DB_CLOSE_DELAY=-1;IGNORECASE=TRUE;DB_CLOSE_ON_EXIT=FALSE

# Schema auto-configuration - drop and recreate the database for each test run
spring.jpa.hibernate.ddl-auto = create-drop
