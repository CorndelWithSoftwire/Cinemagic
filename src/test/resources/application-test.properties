#
# Test-specific Configuration
#

# Use embedded H2 database
spring.datasource.url = jdbc:h2:mem:test;MODE=MYSQL;DB_CLOSE_DELAY=-1;IGNORECASE=TRUE;DB_CLOSE_ON_EXIT=FALSE

# Cinemagic admin password
cinemagic.admin.password = admin_password

# Always include the stack trace in API responses
cinemagic.includeStackTraceInErrorResponse = true
